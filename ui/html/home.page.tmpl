{{define "main"}}
<h2 class="section-title">Local Kazakh Products</h2>
<div class="product-grid"> {{range .Products}}
    <div class="card"> <div class="card-header"><strong>{{.Name}}</strong></div>
        <p class="price-tag">{{.Price}} ₸</p>
        <div class="card-footer">
            <a href="/product?id={{.ID.Hex}}" class="btn-primary">View Details</a>
        </div>
    </div>
    {{else}}
    <p>The marketplace is empty.</p>
    {{end}}
</div>
{{end}}