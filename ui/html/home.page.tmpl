{{define "main"}}
<h2>Local Kazakh Products</h2>
<div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 1rem;">
    {{range .Products}}
    <article class="card">
        <header><strong>{{.Name}}</strong></header>
        <p>{{.Price}} ₸</p>
        <footer>
            <a href="/product?id={{.ID.Hex}}" role="button" class="outline">View Details</a>
        </footer>
    </article>
    {{else}}
    <p>The marketplace is empty.</p>
    {{end}}
</div>
{{end}}